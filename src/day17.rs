use project_root::get_project_root;

const XRANGE: (i32, i32) = (288, 330 + 1);
const YRANGE: (i32, i32) = (-96, -50 + 1 );
pub fn run(day: usize) {
    // let input = std::fs::read_to_string(format!(
    //     "{}/input/input{:02}.txt",
    //     get_project_root().unwrap().to_str().unwrap(),
    //     day
    // ))
    // .unwrap();
}
